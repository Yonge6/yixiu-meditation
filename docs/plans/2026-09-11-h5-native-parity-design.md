# Restore H5 / native parity

Approved scope: restore compact three-column shortcuts below breathing; show 1/3/5/10 minute Focus choices; add local completed-practice journal and Monday-based weekly totals; align free timer/Focus gates with native; use elapsed-time clocks; deploy H5 and install the latest phone-only internal Plus app.

Use the existing deep-water photography, serif titles, teal controls and native hierarchy. Focus scrolls on every viewport and uses normal flow (no absolute orbit overlaps). Me cards use a single 16px gap.

H5 has no verified Apple entitlement session. It remains Free: five nature scenes, two music tracks, 1-minute Focus, and 5/15/30-minute timers. Show locked extended options and explain that Apple purchases/legacy access are restored in the app, never inferred from localStorage. Do not introduce an unverified Plus switch. Native reminders and widgets remain app-specific.

Implement pure clock/journal helpers with validation, bounded 200-entry local storage and no upload. Pauses exclude paused time; delayed ticks reconcile actual elapsed time; completed sessions are recorded exactly once. Changing listening scene or timer starts a new interval, not a mixed-scene record. Preserve playing/paused state across tabs and existing sharing/WeChat routes.

Verify helpers, membership gates, quick presets, one-minute completion/repeat, corrupted storage, consistent card spacing and 390px/iPad portrait/landscape scroll. Run full browser/build/runtime tests. Push source, wait for release workflows, publish a checksummed archive with backup and public asset/DOM readback. Build Debug with DEBUG YIXIU_INTERNAL_PLUS, verify signing, install in-place and read back phone version/launch. No App Store submission is requested.
