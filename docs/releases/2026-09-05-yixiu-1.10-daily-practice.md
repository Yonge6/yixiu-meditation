# Yixiu 1.10 (23): daily practices and a quiet journal

## Scope and source

- User approved reliability + three quick practices + lightweight records, with more refined App/H5 UI.
- Dedicated branch: `codex/yixiu-daily-ritual-20260905`, created from freshly fetched `origin/main` at `7170000cd6fbc2a806cdf4f07c3a3abe5588c630`.
- Preserves the prior unpushed reliability release via cherry-picks (`fead827`, `a455433`, `0a687dc`). The earlier worktrees were not changed.
- Plan: `docs/plans/2026-09-05-daily-ritual.md`.
- Only Yixiu was changed. No Human Design, streak scoring, new account/backend, App Store submission, or unrelated-product promotion.

## Changes

- Focus offers Rain on Eaves / 15 minutes, Stream breathing / 1 minute, and Morning Birds / 5 minutes. These scenes remain free; the new five-minute listening timer is free, while existing Plus gates remain enforced.
- Native playback and breathing use elapsed-time deadlines, preserve fractional pause time, reset when reselecting a duration, and reconcile delayed callbacks. Focus ambience does not double-count a completed listening practice.
- Me shows Monday–Sunday completed activity, this week's completed minutes and the latest three practices with replay. Stores at most 200 completed records locally; no partial-session or historical backfill.
- H5 decodes records defensively, removes invalid/duplicate entries and remains usable when storage is unavailable, with an explicit persistence warning.
- UI reuses the existing water photography and serif/sans pair: photographic practice rows, restrained aqua controls, refined spacing and a quiet editorial journal panel. Sounds remains immersive.
- Daily notification taps route to a ready one-minute Focus practice without unexpected audio. Medium widgets retain the existing breathing action and add a localized link back to the saved sound. `yixiu://focus` and `yixiu://resume` are app-owned routes.
- Native version/build is 1.10 (23). Public Release does not force Plus. Internal Plus continues to require both DEBUG and YIXIU_INTERNAL_PLUS.

## Validation

- `npm run check:runtime`: passed, all 28 protected runtime files unchanged.
- `npm run build` and static production build: passed. Existing >500 kB bundle-size warning remains; no new dependency or media asset was added.
- `npm run test:sites`: 45 passed.
- Full H5 regression: 87 passed (27.2 seconds, two workers). One intermediate run had a protected keyboard gesture timing failure under four workers; an unchanged rerun with two workers passed. Protected runtime code was not modified.
- New browser coverage: all three presets, completed-only persistence/replay, breathing pause/resume/no double count, malformed records, storage denial, and 320×568, 390×844, 1024×1366, 1366×1024 bounds.
- Swift Package: 13 tests passed, including delayed ticks, fractional pauses, repeated start/reset, bounded/deduplicated journal, Codable and local Monday boundaries.
- `AppStatePracticeSmoke.swift`: passed with isolated test preferences and a fake audio engine; checks free presets, real AppState countdown transitions, same-duration reset, no double count, persistence and silent focus routing.
- Existing subscription-access and reminder-manager smoke tests: passed.
- Release iOS Simulator build: passed; app Info.plist reads 1.10 (23). Test app installed/launched on Yixiu-only simulators; native visual inspection is not claimed (Mac locked).
- Chrome local Focus UI was read back. A low-contrast new heading found during inspection was corrected. Phone/iPad bounds are covered by automated H5 tests, not a claim of native device visual QA.

## Remaining external gates

- `gh auth status`: Yonge6 credential invalid. No new remote branch, PR or GitHub Release is claimed.
- `devicectl list devices`: 永歌14PM unavailable.
- Debug device build reached signing but failed with `errSecInternalComponent`; the signing identity is present, and the Mac is locked. No keychain/password/security settings were changed. No 1.10 phone installation is claimed.
- No new App Store upload or review action was taken. Apple download, paid, trial, subscription and revenue outcomes remain unknown, not inferred from building or deployment.

## Reproduce native state verification

```sh
swift test --package-path YixiuMeditation
swiftc -parse-as-library YixiuMeditation/Shared/PracticeJournal.swift YixiuMeditation/YixiuMeditation/Models.swift YixiuMeditation/YixiuMeditation/SubscriptionAccessPolicy.swift YixiuMeditation/YixiuMeditation/AppState.swift YixiuMeditation/Tests/AppStatePracticeSmoke.swift -o /tmp/yixiu-daily-appstate-smoke
/tmp/yixiu-daily-appstate-smoke
```

H5 production receipt and exact resource hashes will be appended only after verified deployment.
